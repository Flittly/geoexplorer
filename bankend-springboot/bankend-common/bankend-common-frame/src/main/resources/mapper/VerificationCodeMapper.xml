<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flittly.bankendspringboot.mapper.VerificationCodeMapper">

    <resultMap id="VerificationCodeResult" type="com.flittly.bankendspringboot.entity.VerificationCode">
        <id property="id" column="id" typeHandler="com.flittly.bankendspringboot.config.UUIDTypeHandler"/>
        <result property="target" column="target"/>
        <result property="code" column="code"/>
        <result property="type" column="type"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="isUsed" column="is_used"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findActiveByTargetAndType" resultMap="VerificationCodeResult">
        SELECT * FROM verification_codes
        WHERE target = #{target} AND type = #{type} AND is_used = false AND expires_at &gt; #{now}
        ORDER BY created_at DESC
    </select>

    <update id="markAsUsed">
        UPDATE verification_codes SET is_used = true
        WHERE target = #{target} AND type = #{type} AND is_used = false
    </update>

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO verification_codes (id, target, code, type, expires_at, is_used, created_at)
        VALUES (#{id, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}, #{target}, #{code}, #{type}, #{expiresAt}, #{isUsed}, #{createdAt})
    </insert>

    <delete id="deleteExpired">
        DELETE FROM verification_codes WHERE expires_at &lt; #{now}
    </delete>

</mapper>



