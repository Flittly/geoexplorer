<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flittly.bankendspringboot.mapper.OrderItemMapper">

    <resultMap id="OrderItemResult" type="com.flittly.bankendspringboot.entity.OrderItem">
        <id property="id" column="id" typeHandler="com.flittly.bankendspringboot.config.UUIDTypeHandler"/>
        <result property="orderId" column="order_id" typeHandler="com.flittly.bankendspringboot.config.UUIDTypeHandler"/>
        <result property="packageId" column="package_id" typeHandler="com.flittly.bankendspringboot.config.UUIDTypeHandler"/>
        <result property="price" column="price"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByOrderId" resultMap="OrderItemResult">
        SELECT * FROM order_items WHERE order_id = #{orderId, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}
    </select>

    <insert id="insert" useGeneratedKeys="false">
        INSERT INTO order_items (id, order_id, package_id, price, created_at)
        VALUES (#{id, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}, #{orderId, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}, #{packageId, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}, #{price}, #{createdAt})
    </insert>

    <insert id="insertBatch">
        INSERT INTO order_items (id, order_id, package_id, price, created_at) VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.id, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}, #{item.orderId, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}, #{item.packageId, typeHandler=com.flittly.bankendspringboot.config.UUIDTypeHandler}, #{item.price}, #{item.createdAt})
        </foreach>
    </insert>

</mapper>
